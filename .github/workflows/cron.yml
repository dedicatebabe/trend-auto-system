# ==========================================
# Version: 2.0.0
# Date: 2026-09-14
# Summary: 旧cronは無効化（bot.yml に移行）
# ==========================================
name: Legacy Cron (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled - use bot.yml"
